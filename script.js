// Setup function runs one time when the program begins
function setup() {
  createCanvas(400, 400);


}

// Draw function runs repeatedly in a loop 
function draw() {
  background(200);
  ellipse(200,200,50)
  
}